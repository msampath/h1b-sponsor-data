{
  "name": "h1b-sponsor-data",
  "version": "0.1.0",
  "description": "Rate-limited public API over six years of aggregated US DOL immigration disclosures (H-1B LCA, PWD, PERM). Cloudflare Workers + R2.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "build": "python etl/build.py",
    "publish": "python etl/publish.py"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
